apiVersion: 1

providers:
  - name: detector
    Folder: Detector
    type: file
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/detector
